# HTTP API schemas

OpenAPI documents will live here when HTTP contracts exist.

Do not invent a product API during bootstrap. `just schema` treats an empty `schemas/api/` tree as “no OpenAPI documents currently defined”.
