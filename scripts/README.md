# Scripts

Repository-owned validators invoked by `just`. Do not duplicate this logic in GitHub Actions or documentation.

Bilingual pairing for README, `docs/`, and Agent Notes is `scripts/verify_bilingual_docs.py` (`just docs-bilingual`).
