Native parser dumps for MinerU / Docling / GROBID adapters.

These are synthetic, self-authored recordings of native schemas. CI maps
them into EvidenceBundle; live model/Java services are optional and off
the default install. See `pdf_pipeline.evidence.mineru` (and siblings).
