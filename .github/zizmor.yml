# GitHub Actions and actionlint both require workspace-relative `./...`
# for same-repo reusable workflows. `$/` is not usable here, so this
# audit is disabled rather than auto-fixed to `$/`.
rules:
  self-repository:
    disable: true
    ignore:
      - ci.yml
      - nightly.yml
