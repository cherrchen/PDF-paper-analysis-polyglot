# Benchmarks

Reserved for PDF parsing latency, layout reconstruction, peak memory, SemanticDocument serialization, LaTeX generation, LaTeX compilation, and PDF output size.

Do not invent numbers here. Add a benchmark when a path is performance-critical and record the method in an Agent Note if the methodology itself is a decision.
